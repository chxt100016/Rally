package com.rally.domain.tournament.roundprogress;

/** The relationship between the persisted current round and the calculated target round. */
public enum RoundProgressDecision {
    NOT_READY,
    STAY,
    ADVANCE
}
