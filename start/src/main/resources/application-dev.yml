spring:
    datasource:
        url: jdbc:mysql://localhost:3306/fan?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
        type: com.zaxxer.hikari.HikariDataSource

wechat:
  miniapp:
    appid: ${WECHAT_APPID:wxa597b7c249b8ff81}
    secret: ${WECHAT_SECRET:dev_secret_placeholder}
    code2session-url: https://api.weixin.qq.com/sns/jscode2session

auth:
  jwt:
    secret: ${JWT_SECRET:dev-jwt-secret-key-at-least-32-bytes!!}
    expire-days: 7

