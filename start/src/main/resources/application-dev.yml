spring:
    datasource:
        url: jdbc:mysql://localhost:3306/fan?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
        type: com.zaxxer.hikari.HikariDataSource

auth:
  jwt:
    secret: ${JWT_SECRET}
    expire-days: 7

qiniu:
  access-key: ${QINIU_ACCESS_KEY}
  secret-key: ${QINIU_SECRET_KEY}
  bucket: rallyo
  domain: https://static.fantasticmonkey.top

wechat:
  mini:
    # 联调期订阅消息跳转开发版；用体验版测试改为 trial
    subscribe-miniprogram-state: developer
