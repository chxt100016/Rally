---
id: platform-config.public-config-query
---

## 服务边界

本服务只向已登录用户按一个或一组原始标识交付当前 JVM 缓存可取得的配置字符串。它不枚举配置，不提供说明、类型、版本、覆盖状态或来源，不解析、校验或修改值，也不保证多运行实例缓存同步。当前没有公开白名单或 `global` 作用域限制：知道完整 `<scope>|<configKey>` 的调用方可直接取得任意作用域的已启用缓存值。
