# platform-config.public-config-query.activity.query-public-config-value 澄清记录

<!-- 本文件由 bp review 命令维护,请勿手工编辑 -->

## 待答复

## 已答复

## 已确认

- [Q1] 单键查询的 key 是否裁剪或受白名单限制？
  > 不裁剪、不限格式长度或名录，空字符串也作为普通标识。
  → 已写入活动契约、详细流程第 1 步与边界情况

- [Q2] 配置查找优先级和作用域行为是什么？
  > 先查原始缓存键，再查 global|key，最后枚举默认；原始值可直接是 scope|configKey，命中任意作用域 enabled 缓存。
  → 已写入业务动作 A1-A3、详细流程第 2-4 步与边界情况

- [Q3] 未知 key 和成功响应元数据如何处理？
  > 缓存及枚举都未命中时成功返回 null；只返回字符串/null，不交付说明、版本、来源或覆盖状态。
  → 已写入活动契约、详细流程第 4-5 步
