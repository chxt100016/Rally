---
id: platform-config.global-config-update
---

## 服务边界

本服务只校验并发布配置名录中的一项 `global` 覆盖值，重新启用并推进其版本，刷新当前运行实例的配置缓存，并返回全量配置视图。它不新增配置名录，不修改其他作用域，不停用或删除覆盖值，不同步其他运行实例，不保存修改人、原值、原因或历史版本，也不保证通用数值符合使用业务的合理范围。
