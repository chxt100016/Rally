---
id: tournament.booking-confirm-admin
---

## 服务边界

本服务由后台运营按 `tournamentId+matchNo` 精确定位一场处于 `SCHEDULED` 的赛事比赛，一次性把全部仍待确认或已被拒绝的参与者改为已确认，全员确认后推进比赛为 `PENDING_PLAY` 并把仍为 `DRAFT` 的关联赛约开放为 `OPEN`；已全部确认时按幂等处理。服务不选定或变更订场人，不创建或修改赛约的时间、场地、费用，不受理拒绝或重订，不记录运营操作人，不发送通知，也不提交或确认赛果、不结算胜负、不推进赛事轮次。
