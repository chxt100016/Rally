---
id: tournament.result-confirmation-timeout
---

## 服务边界

本服务在赛事超时 Job 启用时，批量处理赛果提交后超过 48 小时仍为 `PENDING_CONFIRM` 的比赛：将待确认参与者自动确认，完成比赛，结算胜负方报名并评估赛事轮次。它不提交或改选胜方，不拒绝赛果，不记录比分，不编排下一轮比赛，不发起支付或占位，不发送通知；单场失败不阻断同批其他场。
