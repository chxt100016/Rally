---
id: tournament.tournament-config-update
---

## 服务边界

本服务只由持有后台共享 API Key 的调用方，以完整更新命令修改任意现有自办赛事的可配置字段并事务性保存，同时保留状态、轮次、锁定席位、冠军和关联进度。`offlineFromRound` 可用空值清除并切换为全程线上，其他可选字段暂不支持以空值清除。它不重新同步城市名，不限制赛事状态，不创建、激活、结束或废弃赛事，也不联动报名、比赛、支付、线下活动或匹配。
