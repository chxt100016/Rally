---
id: tournament.tournament-withdraw
---

## 服务边界

本服务把当前用户任意非终态赛事报名置为 `WITHDRAWN`；`CHAMPION`、`WITHDRAWN`、`ELIMINATED` 均拒绝。随后移出赛事讨论，并在存在时终止一场在途比赛、关闭其草稿赛约、将其他仍为 `IN_MATCH` 的参与者退回 `WAITING`。它不检查赛事状态或时间，不改变报名阶段、轮次、参赛编号、搭档关系、偏好和拒赛次数，不删除历史数据，不重新匹配，也不发送通知。服务不处理正赛席位、支付单或退款，当前结果始终表示未触发退款。
