---
id: tournament.booking-reject
---

## 服务边界

本服务覆盖比赛拒绝的五个入口：待选订场人超时、订场人订场超时、非订场方等待订场超时、订场人等待对方确认超时，以及参与者在赛约确认时直接拒赛。前四类按各阶段起点和系统等待配置判定，不累计拒赛次数；直接拒赛不等待超时，但按当前赛段检查并累计本人拒赛次数。成功均终止比赛、记录发起人的拒绝确认、只关闭草稿赛约，并只将仍为 `IN_MATCH` 的报名释放为 `WAITING`。本服务不重新匹配、不退款、不改变非草稿赛约或其他报名状态；提交后通知失败不影响拒赛结果。
