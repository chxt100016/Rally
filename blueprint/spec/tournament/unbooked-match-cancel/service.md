---
id: tournament.unbooked-match-cancel
---

## 服务边界

本服务由运营按赛事一次性物理删除全部 `MATCHED`、`BOOKING` 比赛及参与关系，并把原参与者中仍为 `IN_MATCH` 的报名逐人改回 `WAITING`。它只要求赛事存在且有当前轮次，不检查赛事状态、比赛轮次或是否曾提交赛约；因此重订中的 `BOOKING` 比赛也可能被删除。服务不保留撤销审计，不改变其他报名字段，不收口关联赛约，不通知参赛者，不推进轮次，也不自动重新匹配。
