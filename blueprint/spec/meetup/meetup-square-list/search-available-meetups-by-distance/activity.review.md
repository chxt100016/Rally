# meetup.meetup-square-list.activity.search-available-meetups-by-distance 澄清记录

<!-- 本文件由 bp review 命令维护,请勿手工编辑 -->

## 待答复

## 已答复

## 已确认

- [Q1] 距离查询的候选条件、距离计算和稳定排序是什么？
  > 固定城市、OPEN、未结束且场地坐标非空，其他筛选同时间查询；SQL 用 ST_Distance_Sphere 算米数，按 distance_meters 再 biz_id 升序。
  → 已写入触发条件、活动契约、业务动作 A1-A2、详细流程第 1-4 步

- [Q2] 距离分页为什么全量查询，游标命中、消失和末尾如何处理？
  > 当前 SQL 返回全量排序候选，应用内线性定位 lastBizId 并从下一项截 pageSize+1。空游标从首页；游标消失也从首页；游标为末项时返回空。
  → 已写入业务动作 A3、详细流程第 4-6 步与边界情况

- [Q3] 经纬度、半径和大数据量有哪些当前边界？
  > lng/lat 必须非空但无范围校验，radius 无正数/上限；每页全量距离计算、排序并载入内存，数据增长后成本线性上升且游标项变化可能重复。
  → 已写入异常分支、边界情况与实现提示
