---
id: meetup.meetup-join
---

## 服务边界

本服务校验当前用户资料和约球准入，按约球加入方式建立 `JOINED` 或 `PENDING` 报名；直接加入时同步重算人数、加入群聊，并在提交后直接尝试发送报名或组团通知，待审批时提醒发布者。它不执行审批、自动撤回或退出，不检查其他约球时间冲突，不预占待审批名额，不返回报名编号与终态，也不因通知不可触达或渠道失败撤销报名。
