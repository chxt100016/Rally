---
id: personal-profile.payment-code-save
---

## 服务边界

本服务把登录用户提交的非空白字符串保存为固定扩展键下唯一的当前收款码；无资料时新增，有资料时覆盖值并重新生成业务编号。它不校验账户、档案、资源格式、存在性、图片类型、归属和内容，不上传、签名、返回或删除图片，也不清理被覆盖的旧资源；并发首次保存依赖数据库唯一键，并发覆盖由最后写入者决定。
