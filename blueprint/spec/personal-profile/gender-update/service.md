---
id: personal-profile.gender-update
---

## 服务边界

本服务通过独立入口把登录用户本人的性别覆盖为 `MALE`、`FEMALE` 或 `UNDISCLOSED`，并在同一事务中返回本人聚合档案。它不限制原值、频次、档案状态或既有关系，不记录变更历史，不回写历史比分快照，也不重新审查约球和赛事参与；聚合返回失败会回滚性别保存。其他基础资料可由通用资料修改服务维护。
