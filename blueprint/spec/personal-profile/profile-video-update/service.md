---
id: personal-profile.profile-video-update
---

## 服务边界

本服务按非空白 key 修改登录用户已有网球档案中首个匹配视频的标题，并在同一事务中返回本人聚合档案。目标不存在也成功，重复 key 只改第一项，标题可为空白且展示为“未命名”。它不建档、不上传、替换或删除文件，不改变视频 key、其他视频、档案状态、NTRP、评分和核查信息；聚合失败会回滚修改。
