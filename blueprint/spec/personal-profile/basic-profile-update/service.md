---
id: personal-profile.basic-profile-update
---

## 服务边界

本服务只允许登录用户部分修改本人的昵称、头像资源标识、性别、生日、城市编码和简介，并在同一事务中返回修改后的本人聚合档案。`null` 保留旧值、字符串空串会清空；服务不校验内容、年龄、城市开放性和资源归属，也不建立或改变网球档案、NTRP、评分、状态、统计和视频。返回聚合失败会回滚基础资料保存；上传与删除资源、独立性别修改和自评修改由其他服务负责。
