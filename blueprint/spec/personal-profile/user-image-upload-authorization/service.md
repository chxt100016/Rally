---
id: personal-profile.user-image-upload-authorization
---

## 服务边界

本服务通过头像和指定用途图片两个入口，为登录用户签发精确七牛 key 的一小时上传授权及预览地址。头像使用配置大小和 `.jpeg`，用途图片原样拼接 type、固定 10 MB 与 `.jpg`。它不查账户档案，不接收或验证图片，不确认上传、不持久化 key 或用途、不更新头像，也不清理旧资源；视频授权另属独立服务。
