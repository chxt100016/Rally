# content-production.tournament-image-maintenance.flow.maintain-images-with-sync-statement 澄清记录

<!-- 本文件由 bp review 命令维护,请勿手工编辑 -->

## 待答复

## 已答复

## 已确认

- [Q1] 职业赛事上传入口是否在返回 SQL 前已经自动更新图片绑定？
  > 按 Java 实现确认：是，自动更新已完成；SQL 只是额外交付的手工同步语句，本流程不执行第二次。
  → 已落入触发、接口契约和详细流程。

- [Q2] 没有同编号赛事记录时是否返回 SQL 和资源标识？
  > 按 Java 实现确认：仍返回两个资源标识和 SQL，七牛云图片保留。
  → 已落入详细流程和异常分支。

- [Q3] 相同赛事编号重复上传如何处理？
  > 按 Java 实现确认：沿用固定资源标识上传并覆盖赛事图片绑定，不保留业务版本。
  → 已落入触发。
