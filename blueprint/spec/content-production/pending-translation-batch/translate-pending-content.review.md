# content-production.pending-translation-batch.flow.translate-pending-content 澄清记录

<!-- 本文件由 bp review 命令维护,请勿手工编辑 -->

## 待答复

## 已答复

## 已确认

- [Q1] 一次触发处理多少待译内容？
  > 按 Java 实现确认：先读取当时全部待译条目，再按每批最多 50 条依次处理；处理中新增条目不进入本次初始清单。
  → 已落入触发和详细流程。

- [Q2] 某批 DeepSeek 失败或数量不一致时是否终止整次处理？
  > 按 Java 实现确认：不终止；该批保持待译、不重试，继续下一批。
  → 已落入流程图、详细流程和异常分支。

- [Q3] 单条空白译文和已成功批次如何收场？
  > 按 Java 实现确认：空白译文保持待译；成功批次立即保存并保留，不因后续批次失败回滚。
  → 已落入详细流程和异常分支。
