---
id: transaction-payment.timeout-order-close
---

## 服务边界

本服务定时核实期限已过且仍为 `PENDING` 的支付单：微信确认已付时补记付款并推进赛事报名、正赛席位和轮次，否则关闭本地支付单并尽力关闭微信订单。空期限、未到期和非待支付单不处理；关闭不会改变关联 `PAYING` 报名，后续可重新建单。服务不创建或拉起支付、不接收回执、不退款、对账或通知用户。各对象与渠道操作没有统一事务，条件更新结果未完整校验，并发和异常可能留下本地、赛事与微信状态不一致。
