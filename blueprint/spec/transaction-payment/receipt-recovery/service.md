---
id: transaction-payment.receipt-recovery
---

## 服务边界

本服务定时扫描收到超过五分钟仍为 `RECEIVED` 的支付回执，按关联支付单查询微信，并在确认已付时补记支付单、推进赛事报名、正赛席位和轮次，最后把回执一次性结束为 `PROCESSED` 或 `FAILED`。它不接收或验证原回执，不创建、拉起或关闭支付，不退款或对账，也不再次处理已结束回执。微信未确认已付时回执仍会标为已处理；各对象没有统一事务，异常可能留下部分状态且失败回执没有后续自动重试。
