---
id: transaction-payment.payment-result-receipt
---

## 服务边界

本服务接收微信支付异步回执，对交易事件验签解密并留下处理记录，把首次成功的 `PENDING` 支付单确认为 `PAID`；赛事报名费会进一步占用正赛席位、推进报名和必要的赛事轮次。它不创建支付单、不主动查单、不交付前端付款参数，不处理超时关单、漏回执补偿、退款或对账，也不发送支付通知。未知事件和无需推进的交易会被接受但不表示付款成功；关联业务失败时当前事务捕获异常，可能保留部分状态且同一回执重试无法自动补齐。
