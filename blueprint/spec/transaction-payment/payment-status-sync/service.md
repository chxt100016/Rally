---
id: transaction-payment.payment-status-sync
---

## 服务边界

本服务只允许付款人主动核实自己的支付单并取得最新摘要；本地 `PENDING` 时查询微信，确认首次付款后在同一事务内推进支付单、赛事报名、正赛席位和赛事轮次。已支付、已关闭或已失败的单不再查渠道，微信查单业务失败目前与未支付同样返回 `UNPAID`。它不创建或拉起支付、不接收异步回执、不扫描漏回执、不关单、不退款或对账；已为 `PAID` 但关联业务未推进的异常单也不会由本入口修复。
