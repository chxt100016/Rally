---
id: pro-tour-data.tournament-catalog-collect
---

## 服务边界

本服务只通过未鉴权的 HTTP 入口按年份依次采集 ATP 最多 200 条与 WTA 排除 ITF 后最多 1000 条赛事名录，并按 `(tournamentId, year)` 新增或刷新基础档案、状态和赛期，同时保留既有主图与背景图。它不分页补采，不删除来源遗漏赛事，不采集签表、赛程、实时赛况、赛果或球员排名，不生成图片、翻译或内容，也不提供数量、部分成功明细或跨来源补偿。
