# platform-config.home-config-update 清理确认

## 情况

业务服务 `platform-config.home-config-update` 的业务描述 `blueprint/product/platform-config/home-config-update.md` 不存在,但 `blueprint/spec/platform-config/home-config-update/` 下已有 1 个流程、2 个活动。

**这可能是两种情况之一:**

- product 里的文件被重命名 → 把 `blueprint/spec/platform-config/home-config-update/` 一并改成同样的名字,删除本文件,不要执行下方清理
- 该业务服务已废弃 → 按下方清单执行清理

## 待删除(文档层)

- blueprint/spec/platform-config/home-config-update/  整个目录(含 1 个流程文件与全部活动目录)
- blueprint/codemap/platform-config/home-config-update/  整个目录
- lock.json 中 services["platform-config.home-config-update"]、flows 下 platform-config.home-config-update.flow.update-home-config、activities 下 platform-config.home-config-update.activity.publish-home-config、platform-config.home-config-update.activity.query-home-config-view

## 待删除(代码层,需人再次确认)

- rally-app/src/main/java/com/rally/platform-config/home-config-update/activity/PublishHomeConfigActivity.java
- rally-app/src/main/java/com/rally/platform-config/home-config-query/activity/QueryHomeConfigViewActivity.java
- rally-app/src/main/java/com/rally/system/HomeConfigAdminAppService.java

## 执行后

运行 `node blueprint/scripts/bp.mjs stage` 确认无残留。
